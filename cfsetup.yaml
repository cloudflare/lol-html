cfg: &cfg
  ci-test:
    builddeps:
      - rust
      # required by lazyhtml:
      - build-essential
      - ragel
      - clang
    post-cache:
      - ./scripts/test.sh

stretch: *cfg
jessie: *cfg
